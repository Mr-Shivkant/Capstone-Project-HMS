{
  "version": 2,
  "name": "capstone-project-hms",
  "builds": [
    {
      "src": "api/[...slug].js",
      "use": "@vercel/node"
    },
    {
      "src": "frontend/**/*.{html,css,js}",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/[...slug].js"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/$1"
    }
  ]
}
    }
  ],
  "env": {
    "MONGO_URI": "@MONGO_URI",
    "CLIENT_URL": "@CLIENT_URL"
  }
}
